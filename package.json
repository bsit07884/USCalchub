{
  "name": "uscalchub",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./styles.css --minify && node generate-sitemap.js",
    "watch": "tailwindcss -i ./input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
